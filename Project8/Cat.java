public class Cat extends Pet // Производный класс Cat.
{
    public Cat(String name) // Конструктор класса Cat.
    {
        super(name); // Вызов конструктора базового класса Pet для передачи поля name в конструктор класса Cat.
    }
}